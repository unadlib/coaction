# Subpackage Examples

This directory contains one minimal runnable example for every published subpackage in this monorepo.

Each example exports a `runExample` function that is executed by the e2e suite in `examples/e2e/test/subpackages.e2e.test.ts`.
