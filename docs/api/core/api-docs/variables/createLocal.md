[**coaction**](../../index.md)

---

[coaction](../../modules.md) / [api-docs](../index.md) / createLocal

# Variable: createLocal

> `const` **createLocal**: [`LocalCreator`](../type-aliases/LocalCreator.md)

Defined in: [packages/core/src/createLocal.ts:19](https://github.com/coactionjs/coaction/blob/main/packages/core/src/createLocal.ts#L19)

Create a store without linking the shared transport runtime.

## Remarks

The default `coaction` entry exports this implementation as `create`.
`createLocal` is only its internal and documentation name; it is not
exported by the root `coaction` entry.
